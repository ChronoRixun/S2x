# Slice 6 Quartermaster

774 distinct purchasable GUIDs join itemscollections column0 to collections
column0, using counted members from column3 onward. Column1 completion rewards
are excluded. Generated header ships with the client; research CSVs are not a
runtime dependency. Both captured byte filters100 and150 are supported. Empty
filters return canonical100. Unknown types/IDs return no offers. Page numbers
are one-based with request limit1..100; filtering precedes paging. Short counts
terminate native27B700; a full last page requires an empty following page. Task111
has no NextPageToken field. Empty token is captured; matching decimal page tokens
are additionally accepted, invalid tokens rejected. No supply-drop sales.

Single guessed AC price table: native rarity0..4 -> 50/250/1000/3000/5000. Native
652330 reads item column29. Main-thread table loading publishes recovered rarity;
missing/invalid rarity falls back to common (explicit guess). No retail prices
were found. SKU/product ID=item GUID and numeric terminated skuData are local
metadata policy. All wire fields follow A4A2C0/A4A510/A4A5A0's0x370 layout.

Native cache27B700 and Lua120760 both cap at400. The original cache/buffer are
not expanded.120760 builds a GC-rooted Lua table of all774 GUID strings after
native ready.2797E0 returns stable separately allocated0x2E8 records, initialized
via20D440, with one fixed product item, terminated strings, one price. Its five
recovered callers consume the returned pointer, not a raw-cache index.
274970's separate collection cache hard-codes currency6. Its MP hook returns
catalog price/currency7; operator binding evidence establishes7, not that retail
reader's hard-coded6. These extra paths are required beyond a task111 reply.

Purchasing uses native issuer276580 (task106 remains explicitly unsupported).
A native25-byte transaction generated by8390A0 is returned to Lua11FDF0. Work
runs next main tick so the caller can attach its listener. purchase() atomically
debits7, grants one GUID, stores purchase:<tx> -> GUID:1. Replay after restart
cannot charge/grant twice; conflicting tx, owned items, invalid quantities,
unknown SKUs and insufficient funds fail. Insufficient funds is0x1F47; no mutation.

Recovered SDK success27BA60 reads currency array count+320/pointer+328 and
inventory count+338/pointer+340, updates native caches, callsD5F30/2752E0 and
276220. No SDK success object is fabricated. The local issuer applies the same
absolute wallet/item refresh and calls275360, the shared completion publisher:
inventoryEventType=4, inventoryTaskType=24, success, transaction string. Native
failure276290 also calls275360(false); that UI contract has no numeric error
field. Numeric BD errors remain in -demonware_debug/native_purchase and console.
This bypasses the original task issuer/legacy currency6 preflight for local MP.
Zombies installs none of these hooks and keeps its service branch unchanged.

Release and rebuilt harness/run pass. Tests cover all774 through8 pages,
filtered/mixed/unknown requests, all truncated request prefixes, uint32 page
bounds, native SKU field order, rarity bounds, purchase/no-funds/ownership/replay.
Native callbacks/Lua are decompiled, not executed by the harness. Operator UI
verification remains required; no game launched. Decompiles: ghidra/decomp-slice6.
